# 2026-09-19 数据更新

本次数据来自 WebGL 资源版本 9533，并与仓库原有数据做了结构和覆盖率校验。

## 变化摘要

- 主数据角色从 61 名增加到 72 名，小技能从 151 个增加到 184 个。
- 新增角色：コトノ、イライザ、エアリエル、リコリット、【水着】クルル、【水着】ニナ、【水着】ホノカ、【水着】ヘイリー、ナイア、ユーリ、フェルトゥーナ。
- 小技能的 1–10 级长表从 6,020 条有效运行时对象关联增加到 8,150 条，关联率保持 100%。
- 直接按 Unity PathID 解析两个目录中的 716 个 `AbilityEffectAsset` 和 2,344 条子资源引用；主技能目录 559 个、NetherCode 目录 157 个。两者即使数值 ID 相同也不会再互相覆盖。没有缺失引用，并保留 `SerializeReference` 嵌套条件/参数。
- 角色时间轴从 104 个资源增加到 107 个；106 个包含数据，共 28,879 条原始效果值和 2,110 条汇总记录。
- 画廊新增 5 个此前缺图的资源角色、25 张图片，现有 79 个资源角色、391 张图片。

## 仍待补齐

- 72 个 FC 中，63 个已关联技能级初始选敌信息。其余 9 个需要相应角色运行时资源，其中包含此前就缺失的ルシータ、メリッサ和 7 个新角色。
- 普攻动作循环仍是上一批已导出的 90 个角色资源；本次更新未把“时间轴效果值”误当成实际命中时刻。

原始浏览器缓存、账号信息、Cookie、Token 和完整抓包均未加入仓库。
